# demo 验证环境（RTX 4090 / CUDA 12.4 / Python 3.10）
torch==2.4.0
onnx==1.14.1
onnxruntime-gpu==1.23.2
tensorrt==10.4.0
numpy

# TVM 0.11.1（pip 无官方包，源码编译或）：
#   conda install -c tlcpack -c conda-forge tvm=0.11.1
